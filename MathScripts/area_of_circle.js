"use strict";

//Circle Radius
var radius = 8;

//Formula
var areaOfCircle = Math.PI * radius**2;

//Area result
console.log("The area of a circle with a radius of " + radius + "cm has a area of " + areaOfCircle);